language: ruby

# env:
#   global:
#     - SHIPPABLE_BUNDLER_VERSION=2.0.0

rvm:
  - 2.5

build:
  ci:
    - ruby -v
    - bundle exec rake install
    #- ruby test/test_alpha.rb
    - rake test TEST=test/alpha_test.rb
    - sudo apt-get install tree
    - tree
